{
  "name": "scrum-poker-app",
  "version": "1.0.0",
  "description": "Full-stack Scrum Poker application",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && npm start",
    "build": "npm run build:client && cd server && npm install --production",
    "build:client": "npm install && npm run build:vite",
    "build:vite": "vite build",
    "postinstall": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
